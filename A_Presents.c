#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n;
    scanf("%d", &n);

    int arr[n];
    for (int i = 0; i < n; i++)
    {
        scanf("%d", &arr[n]);
    }

    int next, prev, current;

    for (int i = 0; i < n; i++)
    {
        if (arr[i] == arr[i + 1])
        {
            if (i == sizeof(arr))
            {
                int temp arr[-i]
            }
        }
    }
}