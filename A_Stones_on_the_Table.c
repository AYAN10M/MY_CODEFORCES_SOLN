#include <stdio.h>
#include <string.h>
#include <stdlib.h>
int main()
{
    // number of stone
    int n;
    scanf("%d",&n);

    // array where we will store the data of stone colors
    char s[n+1];
    scanf("%s", s);


    printf("Hello World");
    return 0;
}